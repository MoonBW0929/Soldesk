class Snack2:

    def __init__(self, s_no, s_name, s_date, s_price, s_weight, c_name, c_addr, c_ceo, c_employee):
        self.s_no = s_no
        self.s_name = s_name
        self.s_date = s_date
        self.s_price = int(s_price)
        self.s_weight = int(s_weight)
        self.c_name = c_name
        self.c_addr = c_addr
        self.c_ceo = c_ceo
        self.c_employee = c_employee