class Calculation:

    def cal(x, y):

        a = int(x)
        b = int(y)

        return {
            "plus": a+b,
            "minus": a-b,
            "multiple": a*b,
            "devision": a/b,
        }